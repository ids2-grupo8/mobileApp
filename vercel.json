{
  "buildCommand": "npx expo export --platform web",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/((?!assets/.*|favicon\\.ico|_expo/.*|.*\\.[a-zA-Z0-9]+).*)", "destination": "/" }
  ]
}
